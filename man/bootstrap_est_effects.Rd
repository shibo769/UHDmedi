% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_est_effects.R
\name{bootstrap_est_effects}
\alias{bootstrap_est_effects}
\title{Bootstrap Estimation of Mediation Effects}
\usage{
bootstrap_est_effects(
  X,
  Y,
  M,
  A,
  B = 200,
  logY = FALSE,
  parallel = TRUE,
  nfold1 = 5,
  nfold2 = 3,
  net_alpha = 1,
  zeta = 0.1,
  optimizer = "pogs",
  intercept = FALSE,
  verbose = FALSE,
  max_attempts = 5 * B
)
}
\arguments{
\item{X}{Design matrix of dimension n x p.}

\item{Y}{Outcome vector of length n.}

\item{M}{Mediator matrix of dimension n x q.}

\item{A}{Treatment assignment vector (binary) of length n.}

\item{B}{Integer. Number of bootstrap replications (default = 200).}

\item{logY}{Logical. If TRUE, outcome Y is log-transformed (passed to \code{est_effects()}). Default is FALSE.}

\item{parallel}{Logical. If TRUE, parallel computing is used in \code{est_effects()} (and in each bootstrap iteration).
Default is TRUE.}

\item{nfold1}{Integer. Number of folds for cross-validation in outcome models. Default is 5.}

\item{nfold2}{Integer. Number of folds for cross-validation in mediator models. Default is 3.}

\item{net_alpha}{Numeric. Mixing parameter for elastic net in \code{cv.glmnet} (default is 1 for lasso).}

\item{zeta}{Numeric. Tuning parameter for residual balancing. Default is 0.1.}

\item{optimizer}{Character. Optimizer method for residual balancing. Default is \code{"pogs"}.}

\item{intercept}{Logical. If TRUE, models include an intercept. Default is FALSE.}
}
\value{
A list containing:
\describe{
  \item{summary}{A data.frame summarizing the mean, standard deviation, lower and upper 95\% CI for each effect (TE, NIE, NDE, Y11, Y10, Y00).}
  \item{boot_samples}{A data.frame containing the bootstrap estimates for each replication. Columns include TE, NIE, NDE, Y11, Y10, and Y00.}
}
}
\description{
This function performs a non-parametric bootstrap to estimate the total effect (TE), natural indirect effect (NIE),
and natural direct effect (NDE), as well as intermediate debiased outcome estimates (Y11, Y10, Y00).
For each bootstrap sample, the function resamples the observations (with replacement) and calls \code{est_effects()}.
It then summarizes the bootstrap estimates by computing the mean, standard deviation, and 95\% confidence intervals
(using the 2.5\% and 97.5\% quantiles) for each effect.
}
\examples{
\dontrun{
  # Suppose sim_data is generated from \code{simData()}
  sim_data <- simData()
  boot_results <- bootstrap_est_effects(X = sim_data$X,
                                       Y = sim_data$Y,
                                      M = sim_data$M,
                                      A = sim_data$A,
                                       B = 100,
                                       logY = FALSE,
                                       parallel = TRUE,
                                       nfold1 = 5,
                                       nfold2 = 3,
                                       net_alpha = 1,
                                       zeta = 0.5,
                                       optimizer = "pogs",
                                       intercept = FALSE,
                                       verbose = FALSE)
 
}
}
